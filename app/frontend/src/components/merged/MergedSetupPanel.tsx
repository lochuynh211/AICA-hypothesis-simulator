/**
 * MergedSetupPanel — left-panel stub for the Combined Simulator (020 Task 6).
 * Placeholder only; the real setup controls (trigger plan + service/content
 * package pickers, run-seed, mode) land in a later 020 task.
 */
export default function MergedSetupPanel() {
  return (
    <div data-testid="merged-setup-panel">
      <h2>Setup</h2>
      <p>Combined Simulator setup controls — coming soon.</p>
    </div>
  )
}
