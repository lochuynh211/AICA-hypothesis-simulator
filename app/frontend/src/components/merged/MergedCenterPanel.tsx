/**
 * MergedCenterPanel — center-panel stub for the Combined Simulator (020 Task
 * 6). Placeholder only; the real playback + cockpit + service-proposal view
 * lands in a later 020 task.
 */
export default function MergedCenterPanel() {
  return (
    <div data-testid="merged-center-panel">
      <h2>Playback</h2>
      <p>Combined Simulator playback — coming soon.</p>
    </div>
  )
}
