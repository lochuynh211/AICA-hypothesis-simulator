.agents/
.specify/
.worktrees/
AGENTS.md

.claude/
CLAUDE.md

.codex/
.superpowers/
.codegraph/
.venv/
*.egg-info/
__pycache__/
*.pyc
.mypy_cache/
.pytest_cache/
.ruff_cache/
node_modules/
dist/
build/pyinstaller_work/

artifacts/
.understand-anything/

# Local secrets / live-adapter credentials (never commit)
.env
.env.local

# BYO-key runtime placeholders for networked-surface targets (never commit a key)
*.local.js
key.local.js

# Run evidence logs (only .gitkeep is tracked)
runs/*.json
corporate-proxy.crt

# Music dataset generator — intermediate workspace (never commit)
generation_workspace/
